module.exports = {
  /**
   * Main part of the dependency.
   */
  Sketchub: require('./src/Sketchub'),
  /**
   * Used for internal functions and values, that can be used outside the dependency.
   */
  ApiMeta: require('./src/ApiMeta')
}